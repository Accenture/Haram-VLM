# HARAM-VLM Requirements
# Python 3.9+ required

# Core Deep Learning
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
accelerate>=0.25.0
peft>=0.7.0  # For LoRA implementation

# Vision Models
timm>=0.9.0  # PyTorch Image Models
opencv-python>=4.8.0
pillow>=10.0.0
albumentations>=1.3.0

# Model Optimization
onnx>=1.15.0
onnxruntime>=1.16.0
bitsandbytes>=0.41.0  # For quantization
# tensorrt>=8.6.0  # Optional deployment optimization; install manually (needs matching CUDA)

# Dataset Handling
datasets>=2.15.0
pycocotools>=2.0.7

# Evaluation Metrics
sacrebleu>=2.3.0
rouge-score>=0.1.2
nltk>=3.8.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Experiment Tracking
wandb>=0.16.0
tensorboard>=2.15.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.18.0

# Development Tools
jupyter>=1.0.0
ipykernel>=6.26.0
tqdm>=4.66.0
pytest>=7.4.0
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0

# Utilities
numpy>=1.24.0
pandas>=2.1.0
pyyaml>=6.0.1
jsonlines>=4.0.0
python-dotenv>=1.0.0
click>=8.1.0
rich>=13.7.0  # For beautiful CLI output

# Optional: For advanced features
einops>=0.7.0  # For tensor operations
safetensors>=0.4.0  # For safe model serialization
deepspeed>=0.12.0  # For distributed training
# flash-attn>=2.3.0  # Install separately: needs CUDA 11.6+ and a build toolchain
#   pip install flash-attn --no-build-isolation

# Qwen models are loaded straight from transformers (>=4.57 for Qwen3-VL);
# see requirements_qwen3.txt for that environment.